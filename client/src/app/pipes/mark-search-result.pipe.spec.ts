import { MarkSearchResultPipe } from './mark-search-result.pipe';

describe('MarkSearchResultPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkSearchResultPipe();
    expect(pipe).toBeTruthy();
  });
});
